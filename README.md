# Life Calendar - Ваша жизнь в неделях

Pet-проект для визуализации жизни в неделях, вдохновленный статьей [Wait But Why - Your Life in Weeks](https://waitbutwhy.com/2014/05/life-weeks.html).

## Описание

Приложение позволяет визуализировать вашу жизнь в виде календаря на 90 лет, разбитого по неделям. Каждый год отображается отдельной строкой. Вы можете добавлять события двух типов:

1. **Диапазоны дат** - события с начальной и конечной датой, которые отображаются как цветные блоки на графике
2. **Отдельные даты** - события с конкретной датой, которые отображаются с рамкой и стрелкой к легенде справа

## Технологии

- Vue 3
- TypeScript
- Vite
- CSS3

## Установка и запуск

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev

# Сборка для production
npm run build

# Предпросмотр production сборки
npm run preview
```

## Функциональность

- 📅 Установка даты рождения (начало календаря)
- ➕ Добавление событий (диапазоны и отдельные даты)
- 🎨 Выбор цвета для каждого события
- 💾 Автоматическое сохранение данных в localStorage
- 📤 Экспорт данных в JSON файл
- 📥 Импорт данных из JSON файла
- 🗑️ Удаление событий
- ✏️ Редактирование событий

## Использование

1. При первом запуске установите дату рождения
2. Нажмите "Добавить событие" для создания нового события
3. Выберите тип события (диапазон или отдельная дата)
4. Укажите название, цвет и даты
5. События автоматически отображаются на графике в зависимости от даты рождения

## Структура проекта

```
src/
├── components/
│   ├── BirthDateModal.vue     # Модальное окно для установки даты рождения
│   ├── EventModal.vue         # Модальное окно для добавления/редактирования событий
│   └── LifeCalendar.vue       # Основной компонент календаря
├── utils/
│   ├── dateUtils.ts           # Утилиты для работы с датами
│   └── storage.ts             # Утилиты для работы с localStorage и файлами
├── types.ts                   # TypeScript типы
├── App.vue                    # Главный компонент приложения
├── main.ts                    # Точка входа
└── style.css                  # Глобальные стили
```

## Лицензия

ISC